# Project Mandates

- **Testing:** Always run Playwright tests with `--reporter=line` and `--workers=1`. This ensures results are printed to the console rather than starting a web server for the report and avoids resource contention issues.
